import React from 'react';
import { Typography } from '@mui/material';

const WorkerSupport: React.FC = () => {
  return (
    <>
      <Typography variant="h5" sx={{ mb: 2 }}>Support</Typography>
      <Typography variant="body2" color="text.secondary">Placeholder — support content will go here.</Typography>
    </>
  );
};

export default WorkerSupport;
