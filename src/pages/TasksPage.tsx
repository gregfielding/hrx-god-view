/**
 * Tasks Page
 * 
 * Unified Tasks Hub - A single page where users can see and manage
 * every task assigned to them across all sources.
 */

import React from 'react';
import UnifiedTasksPage from './UnifiedTasksPage';

const TasksPage: React.FC = () => {
  return <UnifiedTasksPage />;
};

export default TasksPage;

