# Deploy log (hrx1-d3beb)

Every Claude session (Greg's or Mark's) appends ONE row here after any
`firebase deploy` — functions, hosting, rules, indexes — then commits and
pushes right away. Before deploying, `git pull` and read the top rows: if the
other person deployed the same function or hosting in the last hour, check
with them first (simultaneous deploys of the same target race).

Newest first. Times are when the deploy finished, Pacific. Commit = the
pushed commit the deploy was built from. Result: ✅ released, ❌ nothing
released (say why).

| When (PT) | Who | Target | What | Commit | Result | Notes |
|---|---|---|---|---|---|---|
| 2026-09-11 16:26 | Greg (Claude) | functions | `syncEntityEmploymentOnboardingFromWorkerOnboarding` | 71cfcb11 | ✅ | The engine sync no longer downgrades C1 Events workers Everee has finished: for entityKey `events`, an Everee-complete worker (row stamps, else the everee_workers link) keeps payroll + tax identity `complete` and `onboardingComplete: true`, so the row stays active. Re-mirroring alone didn't hold — this trigger fires on ANY worker_onboarding write and rewrites those fields from the engine, which never reads Everee (2 rows bounced back to amber within minutes; 150/150 sampled Everee-complete Events workers have a pipeline doc). Other entities unchanged; 3 tests |
| 2026-09-11 16:12 | Greg (Claude) | functions | `natalieSlackInbox` | c46c5dd1 | ✅ | C1 Events follow-up copy (d7625054): checkpoint texts say "finish it at hrxone.com/c1/workers/earnings" (ES "complétalo en …") instead of "the link is in your texts/email" — C1 Events only, every other hiring entity keeps the invite-link wording. Clean origin/main worktree that also contains the other session's cb677df3 (16:05) — additive, not a revert; messaged them before deploying so we didn't 409 |
| 2026-09-11 16:08 | Greg (Claude) | functions | `natalieSlackInbox` | cb677df3 | ✅ | Retry after the 409 — an unknown number that texts an email matching exactly ONE tenant member is now identified and answered directly with their own context (Greg: "have the persona answer those directly… don't bother slack with that"); 2+ matches stay anonymous. Waited for the other session's operation to leave DEPLOYING, then deployed from a clean origin/main worktree; both persona inboxes ticked clean after |
| 2026-09-11 16:05 | Greg (Claude) | functions | `natalieSlackInbox` | cb677df3 | ✅ | "I already did it" re-check (346a1149): a says_done whose item is still open stamps verifyAt +25min; runClaimVerifications re-sends the AccuSource link (or Everee invite) once, or posts ✅ if it cleared. Deployed from origin/main, which already contained the 15:10 Marco deploy (9da2a72d) — newer, not a revert |
| 2026-09-11 16:02 | Greg (Claude) | functions | `natalieSlackInbox` | cb677df3 | ❌ | Unknown-number email identification — HTTP 409 "unable to queue the operation": another session's deploy of the same function was in flight. Nothing released; retrying once that finishes |
| 2026-09-11 15:47 | Greg (Claude) | hosting | C1 Events apply → setup checklist (photo · direct deposit · 1099): quick apply checks setup first, payroll-page card with the next step, jobs-board card 2 → 3 steps, Everee link listener retries after permission-denied | a7eb2cc3 | ✅ | Clean origin/main worktree (contains f2fa927e); preflight rewrites=1, 754 files; postflight 200 on hrxone.com/jobs/job-orders, /c1/jobs-board and app.c1staffing.com/c1/workers/earnings |
| 2026-09-11 15:22 | Greg (Claude) | functions | `scheduledOrchestrator`, `createOffCyclePayment` | 91c7f21c | ❌ | NOT this session's deploy, and nothing was released. Started 09:44 from worktree awesome-yalow-1a1ddb (branch claude/awesome-yalow-1a1ddb, HEAD 91c7f21c, contained in origin/main) and was still sitting there 5h25m later: 2.6s total CPU, 0% CPU, no open network sockets, state S+ — i.e. stuck at a prompt, never uploaded. Killed at 15:22 (pid 2981) with Greg's OK so `npm ci` could rebuild functions/node_modules in the main checkout (5 worktrees symlink to it). Both functions stay on their previously deployed versions — that session must rerun the deploy. Row written by the charming-ishizaka session, which did not run it |
| 2026-09-11 15:10 | Greg (Claude) | functions | `natalieSlackInbox` | 9da2a72d | ✅ | Marco follows up C1 Events applicants who haven't finished setup (new enrollment source `job_application`; C1 Events checklist = photo · direct deposit · W-9, no I-9/W-4) and places them on the shift they applied for as pending — the standard offer text — once done; applicant follow-ups re-check every 10 min. Clean origin/main worktree; also ships the other session's email-subject fix (6c768c62) |
| 2026-09-11 15:04 | Greg (Claude) | functions | `natalieSlackInbox`, `handleInboundSms`, `twilioInboundSmsWebhook` | 0a4e0c45 | ✅ | Workers/applicants texting Natalie's 312 or Marco's 737 get answered by that persona (personaWorkerSms.ts: user search first, own-data context, guardrails, daily Slack log). Clean origin/main worktree; coordinated with the "Claim Shift / C1 Events setup" session, which deploys natalieSlackInbox next |
| 2026-09-11 14:35 | Greg (Claude) | hosting | Step 5 claim-readiness UI: Finish setup to claim CTAs, jobs board setup card, payroll Back to your shift + You're in banners, C1 Events applies land on payroll first | bb5c6c99 | ✅ | Clean worktree of origin/main (contains 010c6894); main.49cabf32.js; deep links 200 on hrxone.com + app.c1staffing.com; released right after respondToAssignment (web calls claim_prepare) |
| 2026-09-11 14:34 | Greg (Claude) | functions | `respondToAssignment` | bb5c6c99 | ✅ | New decision `claim_prepare` (claims/claimReadiness.prepareClaimForWorker): payroll-readiness gate only, starts C1 Events onboarding, books nothing |
| 2026-09-11 14:34 | Greg (Claude) | functions | `natalieSlackInbox` | a361e086 | ✅ | Retry after the Cloud Runtime Config outage — binds Marco's MARCO_SLACK_USER_TOKEN; his inbox initialized 21:30:09Z (30 conversations tracked, no backlog answered), no warnings. Marco now answers Slack DMs/@mentions; his automated work still OFF (app_config/marco.enabled unset) |
| 2026-09-11 14:23 | Greg (Claude) | functions | `natalieSlackInbox` | a361e086 | ❌ | Bind Marco's new MARCO_SLACK_USER_TOKEN version — Google-side outage: "Cloud Runtime Config is currently experiencing issues", nothing released. Retrying from the same clean worktree after 3 min |
| 2026-09-11 14:08 | Greg (Claude) | hosting | Group hiring retired in the UI: group Hiring tab shows a notice; group signup links take the general-signup path (no "approved" copy) | e46cc102 | ✅ | Clean worktree of origin/main (contains f181f80c); main.cc0fd231.js; deep links 200 on hrxone.com + app.c1staffing.com |
| 2026-09-11 14:06 | Greg (Claude) | functions | `onApplicationHiringSignalsChangedAutoOnboard`, `onUserGroupMemberAddedAutoOnboard`, `userGroupHirePassedCandidates`, `validateUserGroupSignup` | e46cc102 | ✅ | S2: C1 Events hires everyone who applies (eventsEntityAutoHire, forward-only) + user-group hiring RETIRED (USER_GROUP_HIRING_RETIRED). The 23 hiring-active groups were flipped to hiringActive=false at 14:03 BEFORE the deploy (backup functions/.scratch/group_hiring_retired_backup_2026-09-11.json); gap check 14:03→deploy: 0 C1 Events applications. Trigger memory → 512MiB |
| 2026-09-11 14:08 | Greg (Claude) | functions | `natalieSlackInbox`, `handleInboundSms`, `twilioInboundSmsWebhook` | 5acdc2ea | ✅ | Staff ↔ persona conversations: staff texting the 312/737 get context-aware answers by text; staff email to connected persona mailboxes (Natalie + Marco) answered in-thread. Clean origin/main worktree (env shasum-matched, --non-interactive) — another session had uncommitted functions/ WIP in the main checkout. No blasts running |
| 2026-09-11 14:03 | Greg (Claude) | functions | `gmailOAuthCallback` | 71549cde | ✅ | Retry of the ❌ above from a CLEAN origin/main worktree (env files copied + shasum-matched, --non-interactive): `marcoMailbox` purpose live — consent URL functions/.scratch/marco-gmail-consent-url.txt |
| 2026-09-11 13:58 | Greg (Claude) | functions | `gmailOAuthCallback` | 0eea199b | ❌ | Predeploy tsc failed on ANOTHER session's uncommitted WIP in the same checkout (untracked src/recruiter/eventsEntityAutoHire.ts) — nothing released. Deploy from a clean origin/main worktree while a teammate session has uncommitted functions/ changes |
| 2026-09-11 13:50 | Greg (Claude) | functions | `natalieSlackInbox`, `handleInboundSms`, `twilioInboundSmsWebhook`, `dispatchScheduledWorkerReminders` | be1bea45 | ✅ | Marco Gomez persona foundation + work routing — OFF until his Slack token + `tenants/{T}/app_config/marco.enabled` (homeChannelId set to #events-recruiting C0C1EEYG820). Live now for Natalie: Flex booking guard (all-digit Venue Smart POs no longer queue Flex bookings), persona SMS From pin (312), bare "Sí" = YES. No scheduled blasts running at deploy. See project_marco_gomez_persona.md |
| 2026-09-11 13:30 | Greg (Claude) | functions | `respondToAssignment` | e9a45af0 | ✅ | Claim Shift payroll-readiness gate (claims/claimReadiness.ts): payroll must be finished at the shift's hiring entity; C1 Events with no employment → onboarding started on the tap (notifications suppressed) + `setup_required`; unfinished → live Everee check then `setup_required`; non-self-serve with no employment → ineligible/not_hired. Memory 256MiB → 512MiB. Deployed right after the hosting release |
| 2026-09-11 13:28 | Greg (Claude) | hosting | Claim sheet understands `setup_required` / `not_hired` (Finish setup → payroll hub) + EN/ES copy | e9a45af0 | ✅ | Clean worktree of origin/main; main.1ea1f0fb.js; deep links 200 on hrxone.com + app.c1staffing.com. Released before respondToAssignment so the web never showed a generic error |
| 2026-09-11 13:40 | Greg (Claude) | hosting | Re-release from origin/main after the Tier 2 interview gate (4faec962) — a no-op for the gate | e45bd1dc | ✅ | Bundle byte-identical to 11:45 (main.c30b0508.js): the web never calls scoreTierPromotion (TierAutomationSettings imports config + labels only), so the gate is tree-shaken out; scorecards the page shows come from server-written proposals. Gate is server-side only (scheduledOrchestrator + scheduledScoringDistribution 13:08). Clean worktree, preflight rewrites=1, 803 files, postflight 200 |
| 2026-09-11 13:09 | Greg (Claude) | functions | `scheduledOrchestrator`, `scheduledScoringDistribution` | b8480560 | ✅ | Tier 2 now requires a completed interview (scoreTierPromotion hard gate, 4faec962) — hourly ramp/hiring-plan promotion + nightly sweep. Built from a clean worktree of origin/main; first attempt from 4faec962 aborted pre-deploy because d3f94fd9 (Sodexo intro) had landed |
| 2026-09-11 13:15 | Greg (Claude) | functions | `natalieSlackInbox` | b8480560 | ✅ | Natalie 1-hour onboarding/screening follow-up check (6b5c7fab). Deployed after the 8 OnTrac Worker Reach blasts finished (574 SMS / 658 push) so no blast was cut off. Clean worktree at origin/main |
| 2026-09-11 12:28 | Greg | functions | `sendJobOrderWorkerReachBlast`, `natalieSlackInbox` | b1fa3afa | ✅ | Worker Reach blasts run on job orders with no shifts (career postings blast the posting; no date, no ?invite=1). Deployed by Greg; both services updated 19:27Z, 8 min after the commit. Logged by Claude |
| 2026-09-11 12:10 | Greg (Claude) | functions | `submitWorkerAiPrescreenInterview`, `reviewAndRescoreUser`, `natalieSlackInbox` | e560c81e | ✅ | Interview scores reach the tier scorecard (riskProfile.topRisks serverTimestamp-in-array fix); Natalie follows job-order hiring-plan hires + never DMs herself on escalations; Craigslist ready+URL → posted (4 OnTrac posts flipped at 19:01Z). Built from a clean worktree (main had another session's uncommitted Sodexo intro work) |
| 2026-09-11 12:04 | Greg (Claude) | functions | `dispatchScheduledWorkerReminders`, `handleInboundSms`, `twilioInboundSmsWebhook` | 6aeefa58 | ✅ | T+15 late check-in gate (Flex punch lookup + feed-freshness guard, canonical-type fix); a NO after start now binds to the started shift (reply handler, so the inbound pair together). All three source zips diffed first: only older committed blobs differed. Built from a worktree with main's env files (shasum-identical), `--non-interactive`. T+30 probe still muted |
| 2026-09-11 11:45 | Greg (Claude) | hosting | Per-host OTP autofill (web passes webOtpHost) + web push dedupe across origins | 55bcb9ea | ✅ | Retry with 8GB heap; main.c30b0508.js; deep links 200 on both hosts |
| 2026-09-11 11:43 | Greg (Claude) | hosting | Per-host OTP autofill (web passes webOtpHost) + web push dedupe across origins | 55bcb9ea | ❌ | Build OOM (fork-ts-checker) — nothing uploaded; retry succeeded 11:45 |
| 2026-09-11 11:36 | Greg (Claude) | functions | `sendOtp` | ba6e24ee | ✅ | SMS WebOTP line echoes the page host when it's ours (utils/webOtpHost.ts); callers that don't send it get PUBLIC_APP_HOST (unchanged behavior) |
| 2026-09-11 10:46 | Greg (Claude) | functions | `triggerAINoteReview`, `triggerAINoteReviewHttp`, `updateLocationAssociationHttp` | 025a811e | ✅ | CORS now echoes app.c1staffing.com; memory 256MiB → 512MiB |
| 2026-09-11 10:41 | Greg (Claude) | functions | `triggerAINoteReviewHttp`, `updateLocationAssociationHttp` | 16f8e975 | ❌ | Cloud Run startup probe failed: 256MiB OOM on cold start. Old revisions kept serving |
| 2026-09-11 10:41 | Greg (Claude) | hosting | Web copy fixes for the app.c1staffing.com move (DeleteAccount, SMSPrivacy, job JSON-LD, agency slug text) | 6e91b73c | ✅ | Built from a clean worktree with `NODE_OPTIONS=--max-old-space-size=8192`; main.9886f869.js; deep link 200 |
| 2026-09-11 10:39 | Greg (Claude) | hosting | Same as above | 16f8e975 | ❌ | Build OOM (fork-ts-checker) while a functions build ran in parallel; nothing uploaded |
| 2026-09-11 09:30 | Greg (Claude) | hosting | Worker "Get the app" banner, shipped OFF | db596f38 | ✅ | main.5b863fa9.js; first attempt minutes earlier OOM'd in the build (nothing uploaded) |
| 2026-09-10 | Greg (Claude) | functions | `checkOtp`, `workerSupportAssistant`, `parseResumeHttp`, `submitWorkerAiPrescreenInterview`, `processApplicantScoreQueue`, `enqueueApplicantScore`, `recalculateApplicantScore` | — | ✅ | AI-processing consent gates (backfilled; time and commit not recorded) |
