# venuesmart travel crew

> "VenueSmart C1 Select travel crew payroll pattern: Mon–Sun custom week, per-event work state (WC dialog picker), $50/day untaxed per-diem Reimbursement column, cross-entity pairing trap"

VenueSmart travel supervisors (Mark True, Kiara Vaughn, Antonio Magana, ~handful) are W-2 through **c1_select_llc** on JO #330 "Venue Smart Supervisors Travel Team" (open-ended assignment `wwFvdPIEMnuA4oaDtp6l__<uid>`, $300/day encoded as 1h @ $300 for day-raters, others hourly). Imported weekly via the **connect_team** xlsx run under the C1 Events context — the assignment's entity override moves their rows to Select (`import.entityOverrideFrom: 'c1_events_llc'`).

Recurring traits (all shipped 2026-08-05):
- **Mon–Sun workweek** ≠ C1 Select's Sun–Sat weekly policy → use the "Custom range…" option in the Timesheets week dropdown (weekly entities drop into manual date pickers; "Back to weeks" returns).
- **Work state varies per event** and the VenueSmart worksite address has NO state (Independence MO doc, state='') → the WC dialog's state picker + worksite/csvSite token sniff ("Mubadala Citi DC Open" → DC) + `workState` stamping on the entry ([[project_wc_classification]]). Rocket Classic = Detroit Golf Club, MI.
- **$50/day food per diem, untaxed** → the grid's Reimb. column (`entry.reimbursementAmount`, edited via setImportEntryExtras) → separate Everee REIMBURSEMENT payable (`…::REIMBURSEMENT` externalId, extras flow in submitImportTimesheetBatch, void retracts it). Never wages, never WC gross.
- **Cross-entity pairing trap**: a single-day C1 Events event assignment (JO #363 Rocket Classic, 7/28-only, same $300 rate) beat the open-ended Select assignment in pairAssignment's "most recent start" tie-break → the row landed under c1_events (hidden from the Select grid, would pay as 1099). Fixed by repointing the entry (scratch fix-mark-728-repoint.ts); watch for recurrence any week a traveler also has a same-rate event assignment.

**Why:** these workers surface every payroll cycle; each trait looks like a bug ("28th missing", "no DC dropdown", "can't include Sunday") but is this pattern.

**How to apply:** for a "missing day," check the other hiring entity first. For WC, pick the event's state in the dialog (8040 Placeholder is always offered, $2.35 synthetic). Per diem goes in Reimb., not Bonus.

**Label note (2026-08-06, commit 93751776):** locked-period payables are now labeled "Wages (N hrs @ $X)" — "Retro wages" read as a duplicate correction on Everee's approval screen and triggered two separate "these are wrong" scares. They still ship payCode BONUS (Everee has no late-wages code). Weekly-routine reminder: submitting the VS upload before Monday's payroll approval avoids the payables detour entirely (Mon–Sat flows as normal worked shifts).

**OT chronology bug (fixed 868356d6, 2026-08-06):** multi-attempt submits landed Sunday 8/02's shifts BEFORE the Mon–Sat retro rows, and `classifyWeeklyOt`'s prior-submission preload was a lump sum that assumed prior = earlier — Sunday's 9–11 hrs pre-banked the 40-hr threshold and slid the OT crossover Thursday→Wednesday ($262.51 OT-premium overpay across the DC trio; corrected pre-approval via `.scratch/fix-vs-week-ot-classification.ts`). Fix: `priorWeekSecondsForBatch` returns per-day REGULAR seconds (no-pyramiding) and the classifier banks each prior day at its chronological position. VS OT anchor: account flag `payWeekStart:'monday'` on `tenants/{t}/accounts/m1JEJs8YPohuXTQVjVQp` (Venuesmart LLC National — the only flagged account); OT workweek is independent of Everee's Sun–Sat pay periods because shifts ship pre-split.

**One-time payment anatomy (verified 2026-08-05, "Mark should get 2450" scare):** the Everee one-time payment after a locked-week submit pools THREE money streams — (1) retro wages as Bonus "Retro wages" (locked days 7/27–8/01 only), (2) all per-diem payables, (3) any PRIOR off-cycle correction payables still pending (Mark's $852+$50 7/24–26 corrections landed in the same payment). The last in-period day (Sun 8/02, open Aug 2–8 period) goes as a normal worked shift and pays on the NEXT REGULAR payroll — it is never in the one-time payment. So Mark's $3,052 = $1,800 retro + $350 per diem + $902 prior corrections, with 8/02's $300 riding the regular run → week total $2,450 exactly as expected. Check payable→payment linkage with `getPayable` (`paymentId`/`payablePaymentRequestId` fields); probe script `.scratch/probe-vs-everee-payables.ts`.

**Orphaned-payable trap:** extras created in a FAILED batch attempt never get payout-requested, and the successful resubmit skips their (alreadyLive) rows — the payable then sits in Everee verified but with `paymentId: NONE`, silently absent from every payment (Antonio's 8/02 per diem). Fix = one-off `requestPayablePayout` with that externalId (`.scratch/payout-antonio-802-perdiem.ts` pattern). Related ledger gap (fixed 17b86138): the retro loop never wrote per-extra status docs, so retro-day per diems were invisible to the HRX ledger; 24 docs backfilled via `.scratch/backfill-retro-perdiem-status-docs.ts`.

**Agreed weekly routine (Greg 2026-08-05 evening):** JO #330 stays the single permanent JO — NO multi-location JOs, NO per-event child JOs (the #363 trap). Assignment = who/whom/pay (invariant); the per-week EVENT worksite rides the entry. Weekly step = Import tab → "Look up worksite" on one row of the new event → account pre-selected (Venue Smart) → "+ New worksite" (name = event label, state pre-sniffed from it, e.g. "Cincinnati OH Open" → OH) → Create & use → applies to all rows of that event (`createTimesheetWorksite` callable creates the location under the account's crm_company). Address/state then drive WC automatically.

**Entity untangle (2026-08-13, `.scratch/untangle-venuesmart-entities.ts`):** on 8/09 the bulk fix-assignment (createImportAssignments) was run with JO #330 selected for 14 NON-traveler event workers (Olmos family, Jackson Burr, etc.) → 14 wrong Select assignments on the travelers shift + 26 Week-of-Aug-2 drafts flipped to c1_select (W-2!) + 28 older sent entries carrying #330 stamps. NO money was wrong — every sent entry had been paid 1099 under c1_events (entityOverrideFrom preserved the original). Repair: 54 entries repointed to per-event C1 Events JOs (venue-verified: Kesha/Toto→COTA #173 Germania, Simple Plan/Young the Giant→Moody #141, WI State Fair→#308, Lolla→#220), 13 events assignments created (no WC → propagation trigger inert), 14 wrong assignments deleted (#330 back to its 6 travelers), NEW JO "Travelers Championship" #228 (dAHFSI1uegYAOq20jW0R, c1_events, TPC River Highlands CT) houses the June golf hours, 11 connect_team site mappings written so future uploads auto-map. Also fixed the tenant jobOrderNumber counter (next was 229 vs max 402 → set 403; duplicate numbers were imminent). RULE: the ONLY VenueSmart workers under Select are the 6 travelers (Karina Perez, Kiara Vaughn, Israel De Julian lira, Antonio Magana, Mark True, Talia Spencer); every other VenueSmart worker = 1099 c1_events on per-event JOs. Watch item: next Connect Team import, confirm travelers still pair to #330 via their open-ended assignments (site mappings should only catch assignment-less workers).
