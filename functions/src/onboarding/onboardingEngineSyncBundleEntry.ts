/**
 * esbuild bundle for worker_onboarding trigger — same engine as Profile Employment (enriched path + sections).
 */
export { computeEntityOnboardingEngineFromBuildContext } from '../../../src/utils/entityOnboardingEngineFromContext';
